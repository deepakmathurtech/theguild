import React from 'react';

export default function EventPlatformNotFound() {
  return (
    <div className="p-6">
      <h3 className="text-lg font-extrabold">Event platform page not found</h3>
      <p className="text-xs text-[var(--text-secondary)] mt-2">
        Use the navigation inside the event platform shell.
      </p>
    </div>
  );
}

